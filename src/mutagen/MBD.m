function bands = MBD(in)

  in = in(:);
  in_length = size(in, 1);
  
  % Synthesize analysis filter kernel
  
  % Get two components: filter output and complement
  % Analyze each component for purity
  % Break down each component that isn't pure or is too wide
  
  % Dissolve or shift band boundaries as appropriate

endfunction